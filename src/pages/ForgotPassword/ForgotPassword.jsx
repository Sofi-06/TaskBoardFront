import { useState } from 'react'
import { apiRequest } from '../../services/api'
import './ForgotPassword.css'
export default function ForgotPassword() { const [email, setEmail] = useState(''); const [sent, setSent] = useState(false); const [error, setError] = useState(''); const submit = async (event) => { event.preventDefault(); setError(''); try { await apiRequest('/auth/forgot-password', { method: 'POST', body: JSON.stringify({ email }) }); setSent(true) } catch (requestError) { setError(requestError.message) } }; return <main className="auth-page"><section className="auth-art"><div className="brand"><span className="brand-mark">T</span><span>TaskBoard</span></div><div className="auth-message"><h1>Vuelve a tener<br />todo bajo control<span>.</span></h1><p>Te ayudamos a recuperar el acceso a tu tablero.</p></div></section><section className="auth-panel"><form className="auth-form" onSubmit={submit}><h2>¿Olvidaste tu contraseña?</h2><p className="subtitle">Escribe tu email y te enviaremos un enlace para recuperarla.</p><label htmlFor="forgot-email">EMAIL</label><input id="forgot-email" type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="sofia@uni.edu" required /><button type="submit">Enviar enlace <span>→</span></button>{sent && <p className="form-success">Revisa tu correo para continuar.</p>}{error && <p className="form-error">{error}</p>}<p className="auth-link"><a href="#login">← Volver a iniciar sesión</a></p></form></section></main> }
