import { useEffect, useState } from 'react'
import { Trash2 } from 'lucide-react'
import Sidebar from '../../components/Sidebar/Sidebar'
import CreateCourseModal from './CreateCourseModal'
import { apiRequest } from '../../services/api'
import '../../components/tasks/NewTaskModal.css'
import './Courses.css'

const fallbackColors = ['#ff7d2b', '#249ecb', '#37a56d', '#e84263']; const dateFormatter = new Intl.DateTimeFormat('es-CO', { day: 'numeric', month: 'short' })
export default function Courses() {
  const [courses, setCourses] = useState([]); const [error, setError] = useState(''); const [showModal, setShowModal] = useState(false); const user = JSON.parse(localStorage.getItem('user') || '{}'); const displayName = user.name || 'Mi perfil'; const loadCourses = () => apiRequest('/courses?limit=100').then((response) => setCourses(response.data || [])).catch((requestError) => setError(requestError.message)); useEffect(() => { loadCourses() }, [])
  const removeCourse = async (event, course) => { event.stopPropagation(); if (!window.confirm(`¿Eliminar el curso "${course.name}"?`)) return; try { await apiRequest(`/courses/${course.id}`, { method: 'DELETE' }); loadCourses() } catch (requestError) { setError(requestError.message) } }
  return <main className="courses-page"><Sidebar active="courses" /><section className="dashboard-content"><header className="topbar"><span>{new Intl.DateTimeFormat('es-CO', { weekday: 'long', day: 'numeric', month: 'long' }).format(new Date())}</span><div><button className="semester">{displayName}</button><span className="profile">{displayName.charAt(0).toUpperCase()}</span></div></header><div className="courses-inner"><div className="courses-heading"><div><h1>Mis cursos</h1><p>{courses.length} {courses.length === 1 ? 'curso' : 'cursos'} en este semestre</p></div><button className="new-task" onClick={() => setShowModal(true)}>＋ Crear curso</button></div>{error && <div className="dashboard-error">No se pudieron cargar tus cursos: {error}</div>}<div className="courses-grid">{courses.map((course, index) => { const nextTask = course.tasks?.find((task) => task.dueDate && task.status !== 'COMPLETED'); const color = course.color || fallbackColors[index % fallbackColors.length]; return <article className="course-page-card" style={{ '--course-color': color }} key={course.id} onClick={() => { window.location.hash = `course/${course.id}` }}><button className="delete-course" title="Eliminar curso" aria-label={`Eliminar ${course.name}`} onClick={(event) => removeCourse(event, course)}><Trash2 size={18} /></button><i /><h2>{course.name}</h2><p>{course.tasks?.length || 0} {course.tasks?.length === 1 ? 'tarea' : 'tareas'} {nextTask ? `· ${nextTask.title} ${dateFormatter.format(new Date(nextTask.dueDate))}` : '· Sin entregas próximas'}</p></article> })}{!courses.length && !error && <p className="empty-courses">Aún no tienes cursos. Crea el primero.</p>}</div></div></section>{showModal && <CreateCourseModal onClose={() => setShowModal(false)} onCreated={() => { setShowModal(false); loadCourses() }} />}</main>
}
